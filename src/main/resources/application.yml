spring:
  profiles:
    active: dev
---
spring:
  config:
    activate:
      on-profile: dev
  data:
    mongodb:
      host: localhost
      port: 27017
      database: local
---

logging:
  level:
    root: info